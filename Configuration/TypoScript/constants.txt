plugin.tx_thrating {
	config {
		# cat=plugin.tx_thrating/enable/b; type=boolean; label=Load jQuery by this extension
		loadJQuery = 1
		# cat=plugin.tx_thrating//b; type=int; label=Map anonymous votings to FEUser Uid (global setting)
		mapAnonymous = 0
		# cat=plugin.tx_thrating/enable/b; type=boolean; label=Info on not logged in user: make message visible (set to 1 - global setting)
		showNoFEUser = 0
	}
	settings {
		# cat=plugin.tx_thrating//a; type=int; label=General storage page where all records are stored
		pluginStoragePid = 0
		# cat=plugin.tx_thrating//a; type=int; label=FE Users and Groups [pid]: Enter the PID where website users and groups are stored
		feUserStoragePid = 0
	}
	view {
		 # cat=plugin.tx_thrating/file; type=string; label=Path to template root (FE)
		templateRootPath = EXT:th_rating/Resources/Private/Templates/
		 # cat=plugin.tx_thrating/file; type=string; label=Path to template partials (FE)
		partialRootPath = EXT:th_rating/Resources/Private/Partials/
		 # cat=plugin.tx_thrating/file; type=string; label=Path to template layouts (FE)
		layoutRootPath = EXT:th_rating/Resources/Private/Layouts/
	}
}
